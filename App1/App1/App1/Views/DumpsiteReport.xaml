<?xml version="1.0" encoding="utf-8" ?>
<ContentPage ﻿<?xml version="1.0" encoding="utf-8" ?>
    <ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EcoBlocApp_test.Views.DumpsiteReportPage"
             xmlns:local="clr-namespace:EcoBlocApp_test.ViewModels"
             Title="Report Dumpsite"
             >
        Title="{Binding Title}">
    
    <ContentPage.BindingContext>
            <local:"EcoBlocApp_test.ViewModels" />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
                <Color> "Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

       < ScrollView>
        <StackLayout >
            <Label Text="Type of waste"

            <Image ="image" />
                        <Button Text="upload pic"
                                Clicked="Button_Clicked" 
                                CornerRadius="25" 
                                BackgroundColor="#25d366"/> >
                <
          
        
                <Button Margin="0,10,0,0" Text="Learn more"
                        Command="{Binding OpenWebCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White" />
            </StackLayout>
        </ScrollView>


</ContentPage>
